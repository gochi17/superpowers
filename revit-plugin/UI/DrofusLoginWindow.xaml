<Window x:Class="SuperpowersRevit.UI.DrofusLoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Connect to Drofus"
        Width="440"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="FontSize"          Value="12"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding"           Value="0"/>
            <Setter Property="Margin"            Value="0,6,10,6"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding"  Value="5,4"/>
            <Setter Property="Margin"   Value="0,4"/>
            <Setter Property="Height"   Value="28"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding"  Value="5,4"/>
            <Setter Property="Margin"   Value="0,4"/>
            <Setter Property="Height"   Value="28"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding"  Value="18,6"/>
            <Setter Property="Margin"   Value="5,0,0,0"/>
            <Setter Property="Height"   Value="30"/>
        </Style>
    </Window.Resources>

    <StackPanel Margin="24">

        <!-- Header -->
        <TextBlock Text="Drofus — Sign In"
                   FontSize="16"
                   FontWeight="SemiBold"
                   Margin="0,0,0,16"/>

        <!-- Fields -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="90"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Host -->
            <Label Grid.Row="0" Grid.Column="0" Content="Host URL"/>
            <TextBox x:Name="HostBox"
                     Grid.Row="0" Grid.Column="1"
                     PlaceholderText="https://yourcompany.drofus.com"/>

            <!-- Username -->
            <Label Grid.Row="1" Grid.Column="0" Content="Username"/>
            <TextBox x:Name="UsernameBox"
                     Grid.Row="1" Grid.Column="1"/>

            <!-- Password -->
            <Label Grid.Row="2" Grid.Column="0" Content="Password"/>
            <PasswordBox x:Name="PasswordBox"
                         Grid.Row="2" Grid.Column="1"/>
        </Grid>

        <!-- Validation error -->
        <TextBlock x:Name="ErrorText"
                   Foreground="Red"
                   FontSize="11"
                   TextWrapping="Wrap"
                   Margin="0,8,0,0"
                   Visibility="Collapsed"/>

        <!-- Buttons -->
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Content="Cancel"
                    IsCancel="True"
                    Click="Cancel_Click"/>
            <Button Content="Connect"
                    IsDefault="True"
                    Background="#0063B1"
                    Foreground="White"
                    Click="Connect_Click"/>
        </StackPanel>

    </StackPanel>
</Window>
